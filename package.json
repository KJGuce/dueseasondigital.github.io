{
  "name": "dueseasondigital",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "cd react-site && npm run dev",
    "build": "cd react-site && npm run build",
    "preview": "cd react-site && npm run preview"
  },
  "dependencies": {
    "@fontsource/inter": "^5.0.0",
    "@fortawesome/fontawesome-free": "^6.5.1"
  }
}
